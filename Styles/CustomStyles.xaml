<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style TargetType="TextBlock" x:Key="CustomUserFormTextBlockStyle">
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="FontFamily" Value="Times New Roman"/>
        <Setter Property="FontSize" Value="15"/>
        <Setter Property="Margin" Value="10 0 10 0" />
        <Setter Property="TextWrapping" Value="Wrap"/>
    </Style>

    <Style TargetType="Border" x:Key="CustomBorderStyle">
        <Setter Property="Background" Value="#B0C4DE"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Padding" Value="20" />
        <Setter Property="Margin" Value="20"/>
    </Style>

    <Style TargetType="Grid" x:Key="CustomGridBackgroundStyle">
        <Setter Property="Background" Value="#778899"/>
    </Style>

    <Style TargetType="Button" x:Key="NormalAnswerButtonStyle">
        <Setter Property="Background" Value="LightGray"/>
    </Style>

    <Style TargetType="Button" x:Key="SelectedAnswerButtonStyle">
        <Setter Property="Background" Value="LightGreen"/>
    </Style>
</ResourceDictionary>